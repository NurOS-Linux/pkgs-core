post_install() {
  /usr/bin/ldconfig -r . 2>/dev/null || /usr/bin/ldconfig 2>/dev/null || true
  if [ -x /usr/bin/locale-gen ]; then
    /usr/bin/locale-gen 2>/dev/null || true
  fi
}

post_upgrade() {
  post_install
}
