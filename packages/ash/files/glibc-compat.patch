diff -uNr sh-5.1.orig/bltin/kill.c sh-5.1/bltin/kill.c
--- sh-5.1.orig/bltin/kill.c
+++ sh-5.1/bltin/kill.c
@@ -107,7 +107,7 @@
 			/* and whether it fits into signals range */
 			if (numsig <= 0 || numsig >= NSIG)
 				nosig(*argv);
-			printf("%s\n", sys_siglist[(int) numsig]);
+			printf("%s\n", sigabbrev_np((int) numsig));
 			exit(0);
 		}
 		printsignals(stdout);
@@ -197,7 +197,7 @@
 	if (strncasecmp(sig, "sig", 3) == 0)
 		sig += 3;
 	for (n = 1; n < NSIG; n++) {
-		if (!strcasecmp(sys_siglist[n], sig))
+		if (!strcasecmp(sigabbrev_np(n), sig))
 			return (n);
 	}
 	return (-1);
@@ -228,7 +228,7 @@
 	}
 
 	for (len = 0, sig = 1; sig < NSIG; sig++) {
-		name = sys_siglist[sig];
+		name = sigabbrev_np(sig);
 		nl = 1 + strlen(name);
 
 		if (len + nl >= termwidth) {
diff -uNr sh-5.1.orig/eval.c sh-5.1/eval.c
--- sh-5.1.orig/eval.c
+++ sh-5.1/eval.c
@@ -52,7 +52,6 @@
 #include <sys/param.h>
 #include <sys/types.h>
 #include <sys/wait.h>
-#include <sys/sysctl.h>
 
 /*
  * Evaluate a command.
diff -uNr sh-5.1.orig/jobs.c sh-5.1/jobs.c
--- sh-5.1.orig/jobs.c
+++ sh-5.1/jobs.c
@@ -462,8 +462,8 @@
 #endif
 				st = WTERMSIG(ps->status);
 			st &= 0x7f;
-			if (st < NSIG && sys_siglist[st])
-				scopyn(sys_siglist[st], s + col, 32);
+			if (st < NSIG && strsignal(st))
+				scopyn(strsignal(st), s + col, 32);
 			else
 				fmtstr(s + col, 16, "Signal %d", st);
 			if (WCOREDUMP(ps->status)) {
diff -uNr sh-5.1.orig/trap.c sh-5.1/trap.c
--- sh-5.1.orig/trap.c
+++ sh-5.1/trap.c
@@ -63,7 +63,6 @@
 #ifdef __linux__
 #define __GNU_SOURCE
 #include <string.h>
-#define sys_signame sys_siglist
 #endif
 
 /*
@@ -106,7 +105,7 @@
 		p += 3;
 
 	for (i = 0; i < NSIG; ++i)
-		if (strcasecmp (p, sys_signame[i]) == 0)
+		if (strcasecmp (p, (sigabbrev_np(i) ? sigabbrev_np(i) : "")) == 0)
 			return i;
 	return -1;
 }
@@ -122,7 +121,7 @@
 	out1str("EXIT ");
 
 	for (n = 1; n < NSIG; n++) {
-		out1fmt("%s", sys_signame[n]);
+		out1fmt("%s", (sigabbrev_np(n) ? sigabbrev_np(n) : ""));
 		if ((n == NSIG/2) ||  n == (NSIG - 1))
 			out1str("\n");
 		else
@@ -147,7 +146,7 @@
 				out1fmt("trap -- ");
 				print_quoted(trap[signo]);
 				out1fmt(" %s\n",
-				    (signo) ? sys_signame[signo] : "EXIT");
+				    (signo) ? (sigabbrev_np(signo) ? sigabbrev_np(signo) : "") : "EXIT");
 			}
 		return 0;
 	}
